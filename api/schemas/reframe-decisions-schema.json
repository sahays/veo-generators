{
  "type": "object",
  "properties": {
    "verdicts": {
      "type": "array",
      "description": "One verdict per decision point, echoing its key.",
      "items": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "The exact key of the decision point being answered."
          },
          "action": {
            "type": "string",
            "enum": ["letterbox", "crop", "follow", "keep_both", "split"],
            "description": "For a text decision: letterbox = keep full width so the side text/graphics stays in frame; crop = follow the speaker (band is just background). For a subject/speaker decision: follow = frame the chosen person (see `subject`). For a speaker decision that offers it: keep_both = frame both people of a genuine two-shot conversation together; split = stack the two as panels (only when offered)."
          },
          "coverage": {
            "type": "number",
            "description": "For a letterbox: fraction of source width (0.0-1.0) that must stay visible to keep the text/graphics. 0 otherwise."
          },
          "subject": {
            "type": "string",
            "enum": ["left", "center", "right"],
            "description": "For a subject decision (action=follow): which visible person is the main subject to frame."
          },
          "reason": {
            "type": "string",
            "description": "Brief justification (what you saw in the thumbnail)."
          }
        },
        "required": ["key", "action"]
      }
    }
  },
  "required": ["verdicts"]
}
